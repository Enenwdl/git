##### Git常用命令

- git config --global user.name xxx
- git config --global user.email xxxx
  - 以上，Git安装好之后必须设置一下用户名，否则无法提交代码
    - 注意：这里设置用户签名和和将来登录远程仓库（或者其他代码托管中心）得账号没有人任何关系。
    - 签名得作用是区分不同操作者身份

- cat ~/.gitconfig:查看用户签名